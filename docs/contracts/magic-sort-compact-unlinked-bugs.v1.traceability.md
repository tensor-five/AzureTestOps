# Kompakte unverbundene Bugs v1 – Traceability-Matrix

| Vertragsanforderung | Eingefrorener Test |
| --- | --- |
| MCU-01 | `MCU-01 and MCU-02 reset stale unlinked slots and keep unlinked Bugs compact in their existing order`; `MCU-01 and MCU-02 apply and persist compact unlinked slots after a Magic Sort click` |
| MCU-02 | `MCU-01 and MCU-02 reset stale unlinked slots and keep unlinked Bugs compact in their existing order`; `MCU-01 and MCU-02 apply and persist compact unlinked slots after a Magic Sort click` |
| MCU-03 | `MCU-03 only moves a connected Bug into a free spacer slot` |
| MCU-04 | `MCU-04 keeps dense Magic Sort unchanged while Add Spacer is disabled` |
| MCU-05 | `MSV-01 and MSV-02 exclude Test Cases inside a collapsed suite from the layout cost`; `MSV-01 ignores a filtered Test Case and preserves the remaining visible layout`; `MSV-01 excludes a connected filtered Bug while optimizing two visible Bugs`; `MMG-01 takes a new DOM snapshot for each separate Magic Sort click`; `MSS-07 renders free slots without Bug cards and animates their vertical movement`; `MS-07 persists and restores both optimized orders for the active set`; `MS-01 exposes the keyboard-operable Magic Sort action with a wand symbol` |
