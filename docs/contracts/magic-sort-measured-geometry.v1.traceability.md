# Magic Sort nach gemessener Geometrie v1 – Traceability-Matrix

| Vertragsanforderung | Eingefrorener Test |
| --- | --- |
| MMG-01 | `MMG-01 and MMG-04 capture visible card centers afresh when Magic Sort starts`; `MMG-01 takes a new DOM snapshot for each separate Magic Sort click` |
| MMG-02 | `MMG-02 minimizes measured card-center distances instead of list indices` |
| MMG-03 | `MMG-03 keeps measured slot centers for suite headers and free Bug slots` |
| MMG-04 | `MMG-04 waits for a Magic Sort click after filter, collapse, and resize changes`; `MS-02 does not reorder after a filter change or a snapshot refresh` |
| MMG-05 | `MMG-05 keeps the current discrete optimization as a fallback without a measurement` |
| MMG-06 | `MS-01 exposes the keyboard-operable Magic Sort action with a wand symbol`; `MS-03 and MS-06 begin a live optimization only after the user triggers Magic Sort`; `MS-04 keeps every test case in its own suite while Magic Sort improves the visible layout`; `MS-07 persists and restores both optimized orders for the active set`; `MSV-01 ignores a filtered Test Case and preserves the remaining visible layout`; `MSV-01 excludes a connected filtered Bug while optimizing two visible Bugs`; `MS-03 starts only for a keyboard-triggered Magic Sort action and uses only the visible items`; `MSS-01 renders the keyboard-operable Add Spacer checkbox beside Magic Sort`; `MSS-02 defaults Add Spacer to disabled and preserves the enabled value when its set is restored`; `MSS-03 retains the dense Bug positions while Add Spacer is disabled`; `MSS-04 through MSS-06 allocate distinct free Bug slots nearest to visible Test Case positions`; `MSS-07 renders free slots without Bug cards and animates their vertical movement`; `MMG-02 minimizes measured card-center distances instead of list indices` |
